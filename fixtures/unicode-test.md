# Unicode probe

```
Hello 世界 🔥 emoji test café naïve Zürich Москва
This paragraph is intentionally long and dense enough to be classified as a dense block by the segmentMarkdown heuristic so that it gets imaged instead of staying as plain prose text in auto mode which only images dense content blocks.
```
